#include<bits/stdc++.h>
//백준 삼성SW역량 12100 2048게임
using namespace std;

const int max_n =24;
int n, a[max_n][max_n], ny,nx;
int dy[4] = {1, 0, -1, 0};
int dx[4] = {0, 1, 0, -1};

int main() {
    scanf("%d", &n);
    for(int i =0; i<n; i++) {
        for(int j = 0; j < n; j++) {
            scanf("%1d", &a[i][j]);
        }
    }

    for(int i = 0; i < 4; i++) {
        int
    }

    return;
}